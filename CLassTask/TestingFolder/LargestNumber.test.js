const {user} = require('../CHAPTER4_Dietel/CLassTask/CHALargestNumber.js')


test('retrieve largest number', () => { 
    let number = 50;
    let quantitySold = 100;
    const largestNumber = salesRecord(number)
    expect(largestNumber).toBe(largestNumber)

 })